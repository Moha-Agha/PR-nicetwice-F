import React from "react";

const ArticlesMotivator = ({ image, tags, title, share, likes, views, youtube, spotify }) => {
  return (
    <div className="article-motivator">
      1
    </div>
  );
};

export default ArticlesMotivator;
